# macOS
.DS_Store

# резервы и временные файлы
*.bak
*.tmp

# парсер: npm-зависимости ставятся в CI, локальный node_modules не нужен в репо
parser/node_modules/

# тестовая выдача парсера (3 рецепта для ревью) — служебный артефакт, не попадает в сайт
recipes-sample.json

# личные заметки и исходники — не публикуем
HANDOFF_для_нового_чата.md
ТЗ.md
*.pdf

# ссылки на отдельный проект расширения Chrome — не часть сайта
chrome-extension/

# старые служебные скрипты интеграций
basket-matcher.js
store-connector.js

# сам deploy-скрипт тоже не публикуем (это личный инструмент)
deploy-to-github.sh
